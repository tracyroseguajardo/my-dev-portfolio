import React from 'react';
import ContactForm from '../components/ContactForm';

const Contact = () => {
    return(
        <main>
            <ContactForm />
        </main>

    );
};

export default Contact;