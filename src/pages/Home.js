import React from 'react';

const Home = () => {
    return(
        <main>
            {/* future development: add a sleek welcome landing page with animations */}
        </main>

    );
};

export default Home;