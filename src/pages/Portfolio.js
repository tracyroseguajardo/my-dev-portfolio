import React from 'react';
import Project from '../components/Project';

const Portfolio = () => {
    return(
        <main>
            <Project />
            
        </main>

    );
};

export default Portfolio;